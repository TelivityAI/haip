{
  "name": "@inhotel-io/connect-gpt",
  "x-inpms-modification-notice": "Modified by inHotel Sàrl for inPMS; see NOTICE.",
  "license": "Apache-2.0",
  "version": "0.0.1",
  "private": true,
  "description": "ChatGPT gateway for inPMS — proxies the inPMS Connect API, serves a ChatGPT-importable OpenAPI 3.1 spec, and logs every tool call (PII-scrubbed) to Supabase.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^20.16.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}
