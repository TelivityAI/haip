# Go live — SendGrid

## Demo
```bash
./integrations/demos/run.sh sendgrid
```
Without SendGrid → SMTP → console.

## Live in a jiffy
1. `SENDGRID_API_KEY` + `SENDGRID_FROM` in `.env`, restart API.
2. Trigger a confirmation email; confirm in SendGrid activity.

Docs: docs/integrations/sendgrid-email.md
