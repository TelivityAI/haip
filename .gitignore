# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.production
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/

# Docker
docker-data/

# Drizzle
drizzle/meta/

# Proprietary — do not commit
kb/
briefs/
instructions/
# Private research & planning artifacts (belt-and-suspenders, never tracked)
**/research/
*.private.md

# Logs
*.log
npm-debug.log*

# Cloudbeds reference manual (private, do not commit)
cloudbeds-guide/
.vercel

# Local IDE / Claude agent config (machine-specific paths; keep out of the public repo)
.claude/

# HAIP AI model-build pipeline — THE MOAT, never commit.
# Data-gen scripts, teacher prompts/weights, QLoRA config, generated dataset, eval
# harness, model weights. Only the final product ships (integration code +
# THIRD_PARTY_LICENSES credits + an Ollama pull reference); the recipe stays private.
model-build/
*.gguf
*.safetensors
training-data/
*.dataset.jsonl
