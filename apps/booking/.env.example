# Publishable booking key (overridden by ?key= or data-booking-key at runtime).
# Defaults to the demo key when unset.
VITE_BOOKING_KEY=pk_live_HAIPDEMO0000000000000000

# Origin override for cross-origin embeds. Leave blank for same-origin (demo).
# VITE_API_BASE=https://pms.example.com
